#include "current_feedback.h"


bool current_fb_init(CurrentFeedback_t *currentFb_inst) {

    switch (currentFb_inst->source) {
        case CURRENT_FB_MOTOR_DRIVER:
            switch (currentFb_inst->driverId) {
                case MOTOR_DRIVER_DRV8317:
            
            }
    }


}